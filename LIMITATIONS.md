# 알려진 제한사항 / 운영 전 확인사항

아래 항목들은 "버그"가 아니라 **아직 정책이 정해지지 않아 구현하지 않은 것**들입니다.
코드를 조용히 바꾸는 대신 여기 남겨둡니다 — 실제로 필요해지면 결정 후 구현할 것.

## 보안
- **세션 강제 무효화 불가**: 세션이 DB가 아니라 서명된 쿠키에만 있어서, 특정 계정을
  지금 당장 로그아웃시킬 방법이 없음 (쿠키 만료 전까지 유효). 필요해지면 세션을 DB에
  저장하는 방식으로 바꿔야 함 (ARCHITECTURE.md 참고).
- **API 요청 제한(rate limit) 없음**: 로그인/번역 등 어떤 엔드포인트에도 호출 빈도 제한이
  없음. 악의적 사용이나 번역 API 비용 급증 우려가 있으면 Cloudflare Rate Limiting 규칙
  추가 필요.
- **운영진 활동 감사 로그 없음**: 누가 언제 회원을 승인/거절했는지, 운영진을 임명/해제했는지
  기록이 안 남음 (D1에 `decided_by`는 있지만 시각순 로그 형태는 아님).

## 개인정보
- **회원 데이터 삭제 절차 없음**: 탈퇴/삭제 요청이 오면 `members`/`users` 테이블에서 수동으로
  지워야 함 — 앱 안에 "회원 삭제" 기능 없음.
- **번역 API로 콘텐츠가 구글에 전송된다는 안내 없음**: 학회원이 쓴 글이 동적 번역을 위해
  Google Translate API로 전송된다는 사실이 사용자에게 고지되지 않음. 필요하면 가입/작성
  화면에 안내 문구 추가.

## 외부 공유 설정 (코드가 아니라 구글 계정 쪽 설정 — 직접 확인 필요)
앱 로그인 화면은 "학회원만 이용 가능"을 안내하지만, 아래는 **앱의 로그인 체크를 거치지
않고 그 자체로 접근 가능한 외부 자원**입니다. 실제 구글 캘린더/드라이브 공유 설정을
직접 확인하세요:
- `CALENDAR_ICS_URL` — 공개(public) 캘린더의 iCal 피드. 이 URL을 아는 사람은 누구나
  일정 내용을 볼 수 있음 (앱 로그인과 무관).
- Google Drive 자료 폴더(링크 공유 방식이라면) — 링크를 아는 사람은 누구나 접근 가능.

민감한 내용을 캘린더/드라이브에 올릴 계획이라면 공유 범위를 좁히거나, 공개가 아닌
비공개 캘린더 + 서비스 계정 전용 접근으로 바꾸는 걸 고려할 것.

## 테스트
자동화된 테스트는 없음 (`.github/workflows/check.yml`은 문법 검사만 함, 동작 검증 아님).
수동으로 확인해야 할 최소 항목:
- 로그인 → 승인 대기 → 운영진 승인 → 학회원 전환
- 운영진 임명/해제 (본인 해제 차단되는지)
- 투표 생성·참여, 마감 처리
- 가입 정원(공지 모집) 초과 시 신청 차단
- 번역 API 키 없이도 앱이 정상 작동하는지 (fallback)
- 캘린더 동기화 실패해도 공지/일정 저장 자체는 성공하는지
